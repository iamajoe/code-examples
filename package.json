{
  "name": "reddit_bedrock",
  "version": "0.0.1",
  "devDependencies": {
    "babel-loader": "^5.3.2",
    "babel-runtime": "^5.8.29",
    "grunt": "^0.4.5",
    "grunt-contrib-copy": "^0.7.0",
    "grunt-contrib-cssmin": "^0.12.1",
    "grunt-contrib-uglify": "^0.7.0",
    "grunt-sass": "^1.0.0",
    "grunt-sass-globbing": "^1.4.0",
    "grunt-webpack": "^1.0.11",
    "json-loader": "^0.5.3",
    "node-sass": "^3.3.3",
    "raw-loader": "^0.5.1",
    "sass": "^0.5.0",
    "webpack": "^1.12.2",
    "express": "^4.13.3",
    "rimraf": "^2.4.3"
  },
  "scripts": {
    "test": "# TODO",
    "build": "node -e \"require('grunt').tasks(['build'], { gruntfile: require('path').join(process.cwd(), 'tasks/build.js') })\"",
    "build-dev": "npm run build dev",
    "build-prod": "npm run build prod",
    "server": "node -e \"require('grunt').tasks(['server'], { gruntfile: require('path').join(process.cwd(), 'tasks/server.js') })\"",
    "start": "npm run build-dev && npm run server",
    "start-prod": "npm run build-prod && npm run server"
  },
  "dependencies": {
    "Bedrock": "git+ssh://git@github.com/Sendoushi/bedrock-web.git",
    "jquery": "^2.1.4",
    "mout": "^0.11.1",
    "underscore": "^1.8.3"
  }
}
